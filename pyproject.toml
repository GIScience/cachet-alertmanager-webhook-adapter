[project]
name = "cachet-adapter"
version = "0.1.0"
description = "A basic adapter from Prometheus' Alertmanager to Cachet"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "email-validator>=2.3.0",
    "fastapi>=0.121.3",
    "pydantic>=2.12.4",
    "pydantic-settings>=2.12.0",
    "requests[socks]>=2.32.5",
    "sqlalchemy>=2.0.45",
    "sqlmodel>=0.0.27",
    "starlette>=0.50.0",
    "urllib3>=2.6.3",
    "uvicorn>=0.38.0",
]

[dependency-groups]
dev = [
    "pre-commit>=4.4.0",
    { include-group = "test" }
]
test = [
    "pytest>=9.0.1",
    "pytest-responses>=0.5.1",
    "httpx>=0.28.1",
    "responses>=0.25.8",
]

[project.scripts]
start-adapter = "cachet_adapter.api:main"
load-components = "cachet_adapter.scripts.load_components:main"
load-dependencies = "cachet_adapter.scripts.load_dependencies:main"

[tool.ruff]
line-length = 120

[tool.ruff.format]
quote-style = "single"

[tool.ruff.lint]
# https://docs.astral.sh/ruff/rules
select = [
    # defaults
    "F",
    "E4", "E7", "E9",
    # additional
    "A", # be sure not to shadow built-in functions
    "I", # isort to sort imports
    "N", # check for use of PEP8 naming conventions
    # docstring cleaning
    "D201", "D207", "D208", "D209", "D210", "D214", "D215", "D300", "D403",
    # auto-remove unused 'noqa' comments
    "RUF100", "RUF101",
]

[build-system]
requires = ["uv_build>=0.11.18,<0.12.0"]
build-backend = "uv_build"
